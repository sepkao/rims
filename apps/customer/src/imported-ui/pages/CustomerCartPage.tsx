import { useNavigate } from 'react-router-dom'

export default function CustomerCartPage() {
  const navigate = useNavigate()
  return <div className="flex min-h-screen justify-center bg-gray-200 font-sans"><div className="relative flex h-screen w-full max-w-[400px] flex-col overflow-hidden bg-[#FDFBF7] shadow-2xl"><header className="flex shrink-0 items-center gap-3 border-b border-[#EAE5DF] bg-white px-5 py-4"><button onClick={() => navigate(-1)} className="rounded-full p-1 text-[#302221] hover:bg-gray-100">←</button><h1 className="text-lg font-bold text-[#302221]">ตะกร้าของฉัน / สถานะ</h1></header><main className="flex-1 overflow-y-auto p-4 pb-[100px]"><section className="mb-8"><h2 className="mb-3 text-sm font-bold text-[#7B726B]">รายการที่กำลังจะสั่ง</h2><EmptyCard message="ยังไม่มีรายการในตะกร้า" detail="กลับไปหน้าเมนูเพื่อเลือกอาหาร" actionLabel="เลือกเมนู" onAction={() => navigate('/order')} /></section><section><h2 className="mb-3 text-sm font-bold text-[#7B726B]">ประวัติการสั่งอาหาร</h2><EmptyCard message="ยังไม่มีประวัติการสั่งซื้อ" detail="รายการสั่งซื้อจะแสดงหลังจากเชื่อมต่อ orders API" /></section></main><footer className="absolute bottom-0 left-0 w-full border-t border-[#EAE5DF] bg-white p-4 shadow-[0_-4px_15px_rgba(0,0,0,0.05)]"><button disabled className="w-full rounded-xl bg-[#5A403E] py-3.5 font-bold text-white opacity-40">ส่งออเดอร์เข้าครัว</button></footer></div></div>
}

function EmptyCard({ message, detail, actionLabel, onAction }: { message: string; detail: string; actionLabel?: string; onAction?: () => void }) { return <div className="rounded-xl border-2 border-dashed border-[#EAE5DF] bg-white p-8 text-center shadow-sm"><p className="font-bold text-[#302221]">{message}</p><p className="mt-1 text-xs text-[#7B726B]">{detail}</p>{actionLabel && <button onClick={onAction} className="mt-5 rounded-lg bg-[#5A403E] px-4 py-2 text-xs font-bold text-white">{actionLabel}</button>}</div> }
